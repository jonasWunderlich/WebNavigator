// Generated by CoffeeScript 1.6.1
(function() {
  var bookMarks, bookmarkIt, bookmartise, context, createBookmark, filter, hSlider, loadBookmarks, loadHistory, processTabConnections, processVisitItems, processed, refArray, reload, renderItem, setConmark, siteContext, siteHistory, siteNetwork, specialise, tabconnections, v_max, vidArray;

  v_max = 5;

  filter = {
    results: 50,
    time: 0,
    query: "",
    mode: "none"
  };

  context = "empty";

  hSlider = 0;

  processed = 0;

  bookMarks = {};

  siteHistory = [];

  tabconnections = [];

  siteNetwork = [];

  siteContext = [];

  vidArray = [];

  refArray = [];

  $(document).ready(function() {
    var max, min, query_slider;
    chrome.storage.local.get("query", function(result) {
      if (result.query != null) {
        filter.query = result.query;
        return $("#search").val(result.query);
      }
    });
    chrome.storage.local.get("hSlider", function(result) {
      if (result.hSlider != null) {
        return query_slider.x = result.hSlider;
      }
    });
    min = 50;
    max = 500;
    query_slider = new Dragdealer('simple-slider', {
      x: hSlider,
      steps: max,
      callback: function(x, y) {
        filter.results = parseInt((max - min) * query_slider.value.current[0] + min);
        chrome.storage.local.set({
          "hSlider": x
        });
        return reload();
      },
      animationCallback: function(x, y) {
        return $("#handle_amount").text(parseInt((max - min) * x + min));
      }
    });
    $("#search").change(function() {
      filter.query = $('#search').val();
      $("#historycontent").empty();
      chrome.storage.local.set({
        "query": filter.query
      });
      return loadHistory();
    });
    chrome.storage.local.get("connections", function(result) {
      if (result.connections != null) {
        tabconnections = result.connections;
      } else {
        tabconnections = [];
      }
      return loadBookmarks();
    });
    return null;
  });

  reload = function() {
    $('#historycontent').empty();
    siteHistory = [];
    bookMarks = {};
    loadBookmarks();
    vidArray = [];
    return siteContext = [];
  };

  loadBookmarks = function() {
    var todo;
    todo = 0;
    return chrome.bookmarks.getTree(function(bookmarkTreeNodes) {
      var m, morebms, n, o, _i, _j, _k, _len, _len1, _len2, _ref, _ref1;
      morebms = bookmarkTreeNodes[0].children[1].children;
      for (_i = 0, _len = morebms.length; _i < _len; _i++) {
        n = morebms[_i];
        if (n.title === "conmarks") {
          _ref = n.children;
          for (_j = 0, _len1 = _ref.length; _j < _len1; _j++) {
            m = _ref[_j];
            todo++;
            if (m.children != null) {
              _ref1 = m.children;
              for (_k = 0, _len2 = _ref1.length; _k < _len2; _k++) {
                o = _ref1[_k];
                todo++;
                bookMarks[o.url] = {
                  context: m.title,
                  id: o.title.split("___", 1)[0],
                  bid: o.id
                };
                todo--;
              }
              todo--;
            } else {
              bookMarks[m.url] = {
                context: void 0,
                id: m.title.split("___", 1)[0],
                bid: m.id
              };
              todo--;
            }
          }
        }
      }
      if (todo === 0) {
        loadHistory();
      }
      return null;
    });
  };

  loadHistory = function() {
    var date, daydate, endtime, microsecondsPerDay, mode, starttime, time;
    time = filter.time;
    mode = filter.mode;
    processed = 0;
    date = new Date();
    daydate = date.getTime() - ((((date.getHours() + 1) * 60 + date.getMinutes()) * 60 + date.getSeconds()) * 1000);
    microsecondsPerDay = 1000 * 60 * 60 * 24;
    endtime = daydate - (microsecondsPerDay * (time - 1));
    starttime = daydate - (microsecondsPerDay * (30 + time));
    chrome.history.search({
      text: filter.query,
      maxResults: filter.results,
      startTime: starttime,
      endTime: endtime
    }, function(historyItems) {
      historyItems.forEach(function(site) {
        processed++;
        return chrome.history.getVisits({
          url: site.url
        }, function(visitItems) {
          return processVisitItems(site, visitItems);
        });
      });
      return null;
    });
    return null;
  };

  processVisitItems = function(site, visitItems) {
    var SiteItem, i, id, ref, referrer, relevance, type, vid, visits, _i, _len;
    id = site.id;
    vid = visitItems[visitItems.length - 1].visitId;
    type = visitItems[visitItems.length - 1].transition;
    ref = visitItems[visitItems.length - 1].referringVisitId;
    relevance = visitItems.length;
    /*----------------------------------------------------------------------------------------
    */

    visits = [];
    referrer = [];
    for (_i = 0, _len = visitItems.length; _i < _len; _i++) {
      i = visitItems[_i];
      visits.push(i.visitId);
      referrer.push(i.referringVisitId);
      vidArray[i.visitId] = {
        sid: site.id
      };
      refArray[i.referringVisitId] = i.visitId;
    }
    siteNetwork[id] = {
      visits: visits,
      referrer: referrer
    };
    /*----------------------------------------------------------------------------------------
    */

    SiteItem = {
      sid: site.id,
      vid: vid,
      url: site.url,
      title: site.title,
      type: type,
      ref: ref,
      relevance: relevance
    };
    siteHistory[id] = SiteItem;
    processed--;
    if (processed === 0) {
      return processTabConnections();
    }
  };

  processTabConnections = function() {
    var id, item, key, referrer, rid, tc, _i, _len, _ref;
    referrer = [];
    referrer[666] = "chrome://newtab/";
    _ref = tabconnections.reverse();
    for (_i = 0, _len = _ref.length; _i < _len; _i++) {
      tc = _ref[_i];
      id = 0;
      rid = tc.refurl;
      for (key in siteHistory) {
        item = siteHistory[key];
        if (item.url === tc.refurl) {
          rid = item.vid;
        }
        if (item.url === tc.url) {
          id = item.vid;
        }
      }
      referrer[id] = rid;
    }
    for (key in siteHistory) {
      item = siteHistory[key];
      if (item.ref === "0" && referrer[item.vid]) {
        item.ref = referrer[item.vid];
      }
    }
    return bookmartise();
  };

  bookmartise = function() {
    var bprocessed, item, key, val, _ref, _results;
    bprocessed = 0;
    for (key in siteHistory) {
      val = siteHistory[key];
      bprocessed++;
      if (bookMarks[val.url]) {
        val.context = bookMarks[val.url].context;
      }
    }
    if (bprocessed = filter.results) {
      siteHistory.sort(function(a, b) {
        if (a.vid >= b.vid) {
          return 1;
        } else {
          return -1;
        }
      });
      console.log(siteNetwork);
      console.log(vidArray);
      console.log(refArray);
      _ref = siteHistory.reverse();
      _results = [];
      for (key in _ref) {
        item = _ref[key];
        _results.push(specialise(item));
      }
      return _results;
    }
  };

  setConmark = function(context, sid) {
    var i, _i, _len, _ref, _results;
    console.log(context + " " + sid);
    _ref = siteNetwork[sid].referrer;
    _results = [];
    for (_i = 0, _len = _ref.length; _i < _len; _i++) {
      i = _ref[_i];
      console.log(vidArray[i]);
      if (i !== "0" && vidArray[i]) {
        siteContext[i] = context + "next";
        _results.push(setConmark(context, vidArray[i].sid));
      } else {
        _results.push(void 0);
      }
    }
    return _results;
  };

  specialise = function(site) {
    var special, title, url;
    url = site.url;
    title = site.title;
    context = void 0;
    special = void 0;
    if (title === "") {
      special = "empty";
      title = url;
    } else {
      if (((url.substr(-4)) === ".jpg") || ((url.substr(-4)) === ".png") || ((url.substr(-5)) === ".jpeg")) {
        special = "image";
        title = "Abbildung";
      } else if ((/youtube/.test(url)) && (/watch/.test(url))) {
        special = "video";
      } else if (/Google-Suche/.test(title)) {
        special = "google";
      }
    }
    site.title = title;
    site.special = special;
    return renderItem(site);
  };

  renderItem = function(item) {
    var bid, button, button2, button3, c_panel, clear, favicon, inhalt, link, pic, qtl, ref, referer, relevance, sid, special, t_panel, title, ttl, type, url, utl, vid, videoframe, y_id;
    url = item.url;
    title = item.title;
    sid = item.sid;
    vid = item.vid;
    type = item.type;
    bid = item.bid;
    special = item.special;
    ref = item.ref;
    relevance = item.relevance;
    context = item.context;
    if (context === void 0) {
      if (siteContext[item.vid] != null) {
        context = siteContext[item.vid];
      }
    }
    t_panel = $("<div>");
    t_panel.addClass("panel");
    t_panel.addClass(type);
    if (ref === "0") {
      t_panel.addClass("refzero");
    }
    t_panel.addClass(special);
    c_panel = $("<div>");
    if (context !== void 0) {
      c_panel.addClass(context);
      c_panel.addClass(vid);
    }
    if (relevance > 20) {
      t_panel.addClass("rel_big");
    } else if (relevance > 5) {
      t_panel.addClass("rel_some");
    } else if (relevance >= 2) {
      t_panel.addClass("rel_twice");
    }
    link = $("<a>");
    link.attr({
      href: url
    });
    link.addClass("urladress");
    inhalt = $("<p>");
    favicon = $("<img>");
    favicon.attr({
      src: "chrome://favicon/" + url
    });
    favicon.addClass("favicon");
    c_panel.append($(favicon));
    referer = $("<p>");
    referer.addClass("referrer");
    referer.text(vid + " <- " + ref);
    if (special !== "google" && special !== "empty") {
      button = $("<button>");
      button.text("1");
      button.click(function() {
        return bookmarkIt(item, "first");
      });
      button2 = $("<button>");
      button2.text("2");
      button2.click(function() {
        return bookmarkIt(item, "second");
      });
      button3 = $("<button>");
      button3.text("3");
      button3.click(function() {
        return bookmarkIt(item, "third");
      });
      c_panel.append($(button));
      c_panel.append($(button2));
      c_panel.append($(button3));
    }
    clear = $("<div>");
    clear.addClass("clear");
    c_panel.append($(clear));
    if (filter.query !== "") {
      qtl = filter.query.toLowerCase();
      ttl = title.toLowerCase();
      utl = url.toLowerCase();
      if (ttl.indexOf(qtl) < 0 && utl.indexOf(qtl) > 0) {
        t_panel.addClass("lessimportant");
      } else if (ttl.indexOf(qtl) < 0 && utl.indexOf(qtl) < 0) {
        t_panel.addClass("unimportant");
      }
    }
    if (special === "image") {
      pic = $("<img>");
      pic.attr({
        src: url.substr(url.search(/http/))
      });
      pic.addClass("imgpreview");
      inhalt.append(pic);
      link.append($(inhalt));
    } else if ((url != null) && special === "video") {
      if (v_max >= 0) {
        videoframe = $("<iframe>");
        videoframe.addClass("youtubevideo");
        y_id = "https://www.youtube.com/embed/" + url.split("watch?v=")[1].split('=')[0].split('&')[0];
        videoframe.attr({
          src: y_id
        });
        c_panel.append(videoframe);
        v_max--;
      } else {
        inhalt.text(title);
        inhalt.attr({
          id: sid
        });
        link.append($(inhalt));
      }
    } else if (special === "google") {
      title = title.split(" - Google-Suche")[0];
      inhalt.text(title);
      inhalt.attr({
        id: sid
      });
      link.append($(inhalt));
    } else {
      inhalt.text(title);
      inhalt.attr({
        id: sid
      });
      link.append($(inhalt));
    }
    t_panel.addClass(special);
    c_panel.append($(link));
    c_panel.append($(referer));
    t_panel.append($(c_panel));
    return $("#historycontent").append($(t_panel));
  };

  bookmarkIt = function(site, context) {
    var url;
    url = site.url;
    context = context;
    if (bookMarks[url] != null) {
      if (bookMarks[url].context === context) {
        return chrome.bookmarks.remove(bookMarks[url].bid, function() {
          return reload();
        });
      } else if (bookMarks[url].context !== void 0) {
        return chrome.bookmarks.remove(bookMarks[url].bid, function() {
          return createBookmark(site, context);
        });
      }
    } else {
      return createBookmark(site, context);
    }
  };

  createBookmark = function(site, context) {
    return chrome.bookmarks.getTree(function(bookmarkTreeNodes) {
      var folder, m, newtitle, sub, _i, _len, _results;
      folder = bookmarkTreeNodes[0].children[1].children;
      newtitle = "" + site.vid + "___" + site.title;
      _results = [];
      for (_i = 0, _len = folder.length; _i < _len; _i++) {
        sub = folder[_i];
        if (sub.title === "conmarks") {
          if (context === void 0) {
            _results.push(chrome.bookmarks.create({
              parentId: file.id,
              title: newtitle,
              url: site.url
            }, function() {
              return reload();
            }));
          } else {
            _results.push((function() {
              var _j, _len1, _ref, _results1;
              _ref = sub.children;
              _results1 = [];
              for (_j = 0, _len1 = _ref.length; _j < _len1; _j++) {
                m = _ref[_j];
                if (context === m.title) {
                  _results1.push(chrome.bookmarks.create({
                    parentId: m.id,
                    title: newtitle,
                    url: site.url
                  }, function() {
                    return reload();
                  }));
                } else {
                  _results1.push(void 0);
                }
              }
              return _results1;
            })());
          }
        } else {
          _results.push(void 0);
        }
      }
      return _results;
    });
  };

}).call(this);
